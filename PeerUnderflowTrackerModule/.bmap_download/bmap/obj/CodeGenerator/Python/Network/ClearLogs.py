#    obj/CodeGenerator/Python/Network/ClearLogs.py
#    Created 27/07/2023 at 10:10:56 from:
#        Messages = messages/Network/ClearLogs.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class ClearLogs :
    ID = 4294967050
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(ClearLogs.MSG_OFFSET + ClearLogs.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, ClearLogs.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, ClearLogs.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(ClearLogs.MSG_OFFSET + ClearLogs.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(ClearLogs.ID)
            self.hdr.SetDataLength(ClearLogs.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Network.ClearLogs"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("Network.ClearLogs", ClearLogs.ID, ClearLogs)
