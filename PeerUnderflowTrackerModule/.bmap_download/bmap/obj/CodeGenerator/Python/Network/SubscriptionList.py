#    obj/CodeGenerator/Python/Network/SubscriptionList.py
#    Created 27/07/2023 at 10:10:56 from:
#        Messages = messages/Network/SubscriptionList.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class SubscriptionList :
    ID = 4294967042
    SIZE = 64
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(SubscriptionList.MSG_OFFSET + SubscriptionList.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, SubscriptionList.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, SubscriptionList.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(SubscriptionList.MSG_OFFSET + SubscriptionList.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(SubscriptionList.ID)
            self.hdr.SetDataLength(SubscriptionList.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Network.SubscriptionList"
    # Accessors
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('4294967295')
    @msg.offset('0')
    @msg.size('4')
    @msg.count(16)
    def GetIDs(self, idx):
        """The IDs of the messages you'd like to receive."""
        value = struct.unpack_from('>L', self.rawBuffer(), SubscriptionList.MSG_OFFSET + 0+idx*4)[0]
        return value
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('4294967295')
    @msg.offset('0')
    @msg.size('4')
    @msg.count(16)
    def SetIDs(self, value, idx):
        """The IDs of the messages you'd like to receive."""
        tmp = min(max(value, 0), 4294967295)
        struct.pack_into('>L', self.rawBuffer(), SubscriptionList.MSG_OFFSET + 0+idx*4, tmp)
    

    # Reflection information
    fields = [ \
        FieldInfo(name="IDs",type="int",units="",minVal="0",maxVal="4294967295",description="The IDs of the messages you'd like to receive.",get=GetIDs,set=SetIDs,count=16, bitfieldInfo = [], enum = [])\
    ]

Messaging.Register("Network.SubscriptionList", SubscriptionList.ID, SubscriptionList)
