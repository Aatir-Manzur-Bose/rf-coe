#    obj/CodeGenerator/Python/Network/QueryLog.py
#    Created 27/07/2023 at 10:10:56 from:
#        Messages = messages/Network/QueryLog.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class QueryLog :
    ID = 4294967049
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(QueryLog.MSG_OFFSET + QueryLog.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, QueryLog.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, QueryLog.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(QueryLog.MSG_OFFSET + QueryLog.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(QueryLog.ID)
            self.hdr.SetDataLength(QueryLog.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Network.QueryLog"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("Network.QueryLog", QueryLog.ID, QueryLog)
