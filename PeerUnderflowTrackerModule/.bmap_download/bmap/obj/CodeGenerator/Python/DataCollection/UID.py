#    obj/CodeGenerator/Python/DataCollection/UID.py
#    Created 27/07/2023 at 10:10:02 from:
#        Messages = messages/DataCollection/UID.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class DataCollection_UID_Get :
    ID = 53329
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 13), ("Function", 5), ("Operator", 1)])
    ReverseIDs = OrderedDict([(13, "FunctionBlock"), (5, "Function"), (1, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(DataCollection_UID_Get.MSG_OFFSET + DataCollection_UID_Get.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, DataCollection_UID_Get.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, DataCollection_UID_Get.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(DataCollection_UID_Get.MSG_OFFSET + DataCollection_UID_Get.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(DataCollection_UID_Get.ID)
            self.hdr.SetDataLength(DataCollection_UID_Get.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "DataCollection.UID.Get"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("DataCollection.UID.Get", DataCollection_UID_Get.ID, DataCollection_UID_Get)
#    obj/CodeGenerator/Python/DataCollection/UID.py
#    Created 27/07/2023 at 10:10:02 from:
#        Messages = messages/DataCollection/UID.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class DataCollection_UID_Status :
    ID = 53331
    SIZE = 16
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 13), ("Function", 5), ("Operator", 3)])
    ReverseIDs = OrderedDict([(13, "FunctionBlock"), (5, "Function"), (3, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(DataCollection_UID_Status.MSG_OFFSET + DataCollection_UID_Status.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, DataCollection_UID_Status.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, DataCollection_UID_Status.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(DataCollection_UID_Status.MSG_OFFSET + DataCollection_UID_Status.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(DataCollection_UID_Status.ID)
            self.hdr.SetDataLength(DataCollection_UID_Status.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "DataCollection.UID.Status"
    # Accessors
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(16)
    def GetHandle(self, idx):
        """A 128-bit number, in binary format (MSB-LSB)"""
        value = struct.unpack_from('B', self.rawBuffer(), DataCollection_UID_Status.MSG_OFFSET + 0+idx*1)[0]
        return value
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(16)
    def SetHandle(self, value, idx):
        """A 128-bit number, in binary format (MSB-LSB)"""
        tmp = min(max(value, 0), 255)
        struct.pack_into('B', self.rawBuffer(), DataCollection_UID_Status.MSG_OFFSET + 0+idx*1, tmp)
    

    # Reflection information
    fields = [ \
        FieldInfo(name="Handle",type="int",units="",minVal="0",maxVal="255",description="A 128-bit number, in binary format (MSB-LSB)",get=GetHandle,set=SetHandle,count=16, bitfieldInfo = [], enum = [])\
    ]

Messaging.Register("DataCollection.UID.Status", DataCollection_UID_Status.ID, DataCollection_UID_Status)
