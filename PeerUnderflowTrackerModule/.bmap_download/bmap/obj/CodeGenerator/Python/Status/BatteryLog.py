#    obj/CodeGenerator/Python/Status/BatteryLog.py
#    Created 27/07/2023 at 10:11:16 from:
#        Messages = messages/Status/BatteryLog.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Status_BatteryLog_Get :
    ID = 8401
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 2), ("Function", 13), ("Operator", 1)])
    ReverseIDs = OrderedDict([(2, "FunctionBlock"), (13, "Function"), (1, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Status_BatteryLog_Get.MSG_OFFSET + Status_BatteryLog_Get.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Status_BatteryLog_Get.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Status_BatteryLog_Get.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Status_BatteryLog_Get.MSG_OFFSET + Status_BatteryLog_Get.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Status_BatteryLog_Get.ID)
            self.hdr.SetDataLength(Status_BatteryLog_Get.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Status.BatteryLog.Get"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("Status.BatteryLog.Get", Status_BatteryLog_Get.ID, Status_BatteryLog_Get)
#    obj/CodeGenerator/Python/Status/BatteryLog.py
#    Created 27/07/2023 at 10:11:16 from:
#        Messages = messages/Status/BatteryLog.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Status_BatteryLog_Status :
    ID = 8403
    SIZE = 254
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 2), ("Function", 13), ("Operator", 3)])
    ReverseIDs = OrderedDict([(2, "FunctionBlock"), (13, "Function"), (3, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Status_BatteryLog_Status.MSG_OFFSET + Status_BatteryLog_Status.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Status_BatteryLog_Status.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Status_BatteryLog_Status.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Status_BatteryLog_Status.MSG_OFFSET + Status_BatteryLog_Status.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Status_BatteryLog_Status.ID)
            self.hdr.SetDataLength(Status_BatteryLog_Status.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Status.BatteryLog.Status"
    # Accessors
    @msg.units('ASCII')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(254)
    def GetLogMessages(self):
        """Battery log (UTF-8 String)"""
        count = 254
        if count > len(self.rawBuffer())-(Status_BatteryLog_Status.MSG_OFFSET + 0):
            count = len(self.rawBuffer())-(Status_BatteryLog_Status.MSG_OFFSET + 0)
    
        value = struct.unpack_from(str(count)+'s', self.rawBuffer(), Status_BatteryLog_Status.MSG_OFFSET + 0)[0]
        ascii_len = str(value).find("\\x00")
        value = str(value)[2:ascii_len]
        return value
    
    @msg.units('ASCII')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(254)
    def SetLogMessages(self, value):
        """Battery log (UTF-8 String)"""
        tmp = value.encode('utf-8')
        struct.pack_into('254s', self.rawBuffer(), Status_BatteryLog_Status.MSG_OFFSET + 0, tmp)
    

    # Reflection information
    fields = [ \
        FieldInfo(name="LogMessages",type="string",units="ASCII",minVal="0",maxVal="255",description="Battery log (UTF-8 String)",get=GetLogMessages,set=SetLogMessages,count=1, bitfieldInfo = [], enum = [])\
    ]

Messaging.Register("Status.BatteryLog.Status", Status_BatteryLog_Status.ID, Status_BatteryLog_Status)
