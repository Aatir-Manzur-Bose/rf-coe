#    obj/CodeGenerator/Python/Status/ChargerDetect.py
#    Created 27/07/2023 at 10:11:16 from:
#        Messages = messages/Status/ChargerDetect.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Status_ChargerDetect_Get :
    ID = 8273
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 2), ("Function", 5), ("Operator", 1)])
    ReverseIDs = OrderedDict([(2, "FunctionBlock"), (5, "Function"), (1, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Status_ChargerDetect_Get.MSG_OFFSET + Status_ChargerDetect_Get.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Status_ChargerDetect_Get.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Status_ChargerDetect_Get.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Status_ChargerDetect_Get.MSG_OFFSET + Status_ChargerDetect_Get.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Status_ChargerDetect_Get.ID)
            self.hdr.SetDataLength(Status_ChargerDetect_Get.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Status.ChargerDetect.Get"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("Status.ChargerDetect.Get", Status_ChargerDetect_Get.ID, Status_ChargerDetect_Get)
#    obj/CodeGenerator/Python/Status/ChargerDetect.py
#    Created 27/07/2023 at 10:11:16 from:
#        Messages = messages/Status/ChargerDetect.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Status_ChargerDetect_Status :
    ID = 8275
    SIZE = 1
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 2), ("Function", 5), ("Operator", 3)])
    ReverseIDs = OrderedDict([(2, "FunctionBlock"), (5, "Function"), (3, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Status_ChargerDetect_Status.MSG_OFFSET + Status_ChargerDetect_Status.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Status_ChargerDetect_Status.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Status_ChargerDetect_Status.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Status_ChargerDetect_Status.MSG_OFFSET + Status_ChargerDetect_Status.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Status_ChargerDetect_Status.ID)
            self.hdr.SetDataLength(Status_ChargerDetect_Status.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Status.ChargerDetect.Status"
    # Accessors
    @msg.units('Boolean')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def GetChargerPlugged(self):
        """0-Charger unplugged, 1-Charger plugged in"""
        value = struct.unpack_from('B', self.rawBuffer(), Status_ChargerDetect_Status.MSG_OFFSET + 0)[0]
        return value
    
    @msg.units('Boolean')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def SetChargerPlugged(self, value):
        """0-Charger unplugged, 1-Charger plugged in"""
        tmp = min(max(value, 0), 255)
        struct.pack_into('B', self.rawBuffer(), Status_ChargerDetect_Status.MSG_OFFSET + 0, tmp)
    

    # Reflection information
    fields = [ \
        FieldInfo(name="ChargerPlugged",type="int",units="Boolean",minVal="0",maxVal="255",description="0-Charger unplugged, 1-Charger plugged in",get=GetChargerPlugged,set=SetChargerPlugged,count=1, bitfieldInfo = [], enum = [])\
    ]

Messaging.Register("Status.ChargerDetect.Status", Status_ChargerDetect_Status.ID, Status_ChargerDetect_Status)
