#    obj/CodeGenerator/Python/Experimental/BoseActivityRecognition.py
#    Created 27/07/2023 at 10:10:30 from:
#        Messages = messages/Experimental/BoseActivityRecognition.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Experimental_BoseActivityRecognition_Status :
    ID = 78227
    SIZE = 1
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    HeadphonePositions = OrderedDict([("Invalid", 0), ("OnHead", 1), ("OnShoulder", 2), ("OnTable", 3)])
    ReverseHeadphonePositions = OrderedDict([(0, "Invalid"), (1, "OnHead"), (2, "OnShoulder"), (3, "OnTable")])
    IDs = OrderedDict([("FunctionBlock", 19), ("Function", 25), ("Operator", 3)])
    ReverseIDs = OrderedDict([(19, "FunctionBlock"), (25, "Function"), (3, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Experimental_BoseActivityRecognition_Status.MSG_OFFSET + Experimental_BoseActivityRecognition_Status.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Experimental_BoseActivityRecognition_Status.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Experimental_BoseActivityRecognition_Status.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Experimental_BoseActivityRecognition_Status.MSG_OFFSET + Experimental_BoseActivityRecognition_Status.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Experimental_BoseActivityRecognition_Status.ID)
            self.hdr.SetDataLength(Experimental_BoseActivityRecognition_Status.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Experimental.BoseActivityRecognition.Status"
    # Accessors
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def GetPosition(self, enumAsInt=0):
        """"""
        value = struct.unpack_from('B', self.rawBuffer(), Experimental_BoseActivityRecognition_Status.MSG_OFFSET + 0)[0]
        if not enumAsInt:
            value = Experimental_BoseActivityRecognition_Status.ReverseHeadphonePositions.get(value, value)
        return value
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def SetPosition(self, value):
        """"""
        defaultValue = 0
        try:
            value = int(float(value))
        except ValueError:
            pass
        if isinstance(value, int) or value.isdigit():
            defaultValue = int(value)
        value = Experimental_BoseActivityRecognition_Status.HeadphonePositions.get(value, defaultValue)
        tmp = min(max(value, 0), 255)
        struct.pack_into('B', self.rawBuffer(), Experimental_BoseActivityRecognition_Status.MSG_OFFSET + 0, tmp)
    

    # Reflection information
    fields = [ \
        FieldInfo(name="Position",type="enumeration",units="",minVal="0",maxVal="255",description="",get=GetPosition,set=SetPosition,count=1, bitfieldInfo = [], enum = [HeadphonePositions, ReverseHeadphonePositions])\
    ]

Messaging.Register("Experimental.BoseActivityRecognition.Status", Experimental_BoseActivityRecognition_Status.ID, Experimental_BoseActivityRecognition_Status)
