#    obj/CodeGenerator/Python/ProductInfo/FirmwareVersion.py
#    Created 27/07/2023 at 10:11:00 from:
#        Messages = messages/ProductInfo/FirmwareVersion.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class ProductInfo_FirmwareVersion_Get :
    ID = 81
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 0), ("Function", 5), ("Operator", 1)])
    ReverseIDs = OrderedDict([(0, "FunctionBlock"), (5, "Function"), (1, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(ProductInfo_FirmwareVersion_Get.MSG_OFFSET + ProductInfo_FirmwareVersion_Get.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, ProductInfo_FirmwareVersion_Get.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, ProductInfo_FirmwareVersion_Get.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(ProductInfo_FirmwareVersion_Get.MSG_OFFSET + ProductInfo_FirmwareVersion_Get.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(ProductInfo_FirmwareVersion_Get.ID)
            self.hdr.SetDataLength(ProductInfo_FirmwareVersion_Get.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "ProductInfo.FirmwareVersion.Get"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("ProductInfo.FirmwareVersion.Get", ProductInfo_FirmwareVersion_Get.ID, ProductInfo_FirmwareVersion_Get)
#    obj/CodeGenerator/Python/ProductInfo/FirmwareVersion.py
#    Created 27/07/2023 at 10:11:00 from:
#        Messages = messages/ProductInfo/FirmwareVersion.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class ProductInfo_FirmwareVersion_Status :
    ID = 83
    SIZE = 255
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 0), ("Function", 5), ("Operator", 3)])
    ReverseIDs = OrderedDict([(0, "FunctionBlock"), (5, "Function"), (3, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(ProductInfo_FirmwareVersion_Status.MSG_OFFSET + ProductInfo_FirmwareVersion_Status.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, ProductInfo_FirmwareVersion_Status.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, ProductInfo_FirmwareVersion_Status.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(ProductInfo_FirmwareVersion_Status.MSG_OFFSET + ProductInfo_FirmwareVersion_Status.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(ProductInfo_FirmwareVersion_Status.ID)
            self.hdr.SetDataLength(ProductInfo_FirmwareVersion_Status.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "ProductInfo.FirmwareVersion.Status"
    # Accessors
    @msg.units('ASCII')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(255)
    def GetVersion(self):
        """Firmware Version (UTF-8 String). N is considered as 1-255 bytes here."""
        count = 255
        if count > len(self.rawBuffer())-(ProductInfo_FirmwareVersion_Status.MSG_OFFSET + 0):
            count = len(self.rawBuffer())-(ProductInfo_FirmwareVersion_Status.MSG_OFFSET + 0)
    
        value = struct.unpack_from(str(count)+'s', self.rawBuffer(), ProductInfo_FirmwareVersion_Status.MSG_OFFSET + 0)[0]
        ascii_len = str(value).find("\\x00")
        value = str(value)[2:ascii_len]
        return value
    
    @msg.units('ASCII')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(255)
    def SetVersion(self, value):
        """Firmware Version (UTF-8 String). N is considered as 1-255 bytes here."""
        tmp = value.encode('utf-8')
        struct.pack_into('255s', self.rawBuffer(), ProductInfo_FirmwareVersion_Status.MSG_OFFSET + 0, tmp)
    

    # Reflection information
    fields = [ \
        FieldInfo(name="Version",type="string",units="ASCII",minVal="0",maxVal="255",description="Firmware Version (UTF-8 String). N is considered as 1-255 bytes here.",get=GetVersion,set=SetVersion,count=1, bitfieldInfo = [], enum = [])\
    ]

Messaging.Register("ProductInfo.FirmwareVersion.Status", ProductInfo_FirmwareVersion_Status.ID, ProductInfo_FirmwareVersion_Status)
