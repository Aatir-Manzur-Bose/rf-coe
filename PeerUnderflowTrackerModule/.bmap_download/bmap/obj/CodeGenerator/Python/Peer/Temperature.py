#    obj/CodeGenerator/Python/Peer/Temperature.py
#    Created 27/07/2023 at 10:10:58 from:
#        Messages = messages/Peer/Temperature.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Peer_Temperature_Get :
    ID = 61489
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 15), ("Function", 3), ("Operator", 1)])
    ReverseIDs = OrderedDict([(15, "FunctionBlock"), (3, "Function"), (1, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Peer_Temperature_Get.MSG_OFFSET + Peer_Temperature_Get.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Peer_Temperature_Get.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Peer_Temperature_Get.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Peer_Temperature_Get.MSG_OFFSET + Peer_Temperature_Get.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Peer_Temperature_Get.ID)
            self.hdr.SetDataLength(Peer_Temperature_Get.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Peer.Temperature.Get"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("Peer.Temperature.Get", Peer_Temperature_Get.ID, Peer_Temperature_Get)
#    obj/CodeGenerator/Python/Peer/Temperature.py
#    Created 27/07/2023 at 10:10:58 from:
#        Messages = messages/Peer/Temperature.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Peer_Temperature_Status :
    ID = 61491
    SIZE = 1
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 15), ("Function", 3), ("Operator", 3)])
    ReverseIDs = OrderedDict([(15, "FunctionBlock"), (3, "Function"), (3, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Peer_Temperature_Status.MSG_OFFSET + Peer_Temperature_Status.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Peer_Temperature_Status.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Peer_Temperature_Status.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Peer_Temperature_Status.MSG_OFFSET + Peer_Temperature_Status.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Peer_Temperature_Status.ID)
            self.hdr.SetDataLength(Peer_Temperature_Status.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Peer.Temperature.Status"
    # Accessors
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def GetTemperature(self):
        """Temperature in degrees C (signed byte, -128 - +127)"""
        value = struct.unpack_from('B', self.rawBuffer(), Peer_Temperature_Status.MSG_OFFSET + 0)[0]
        return value
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def SetTemperature(self, value):
        """Temperature in degrees C (signed byte, -128 - +127)"""
        tmp = min(max(value, 0), 255)
        struct.pack_into('B', self.rawBuffer(), Peer_Temperature_Status.MSG_OFFSET + 0, tmp)
    

    # Reflection information
    fields = [ \
        FieldInfo(name="Temperature",type="int",units="",minVal="0",maxVal="255",description="Temperature in degrees C (signed byte, -128 - +127)",get=GetTemperature,set=SetTemperature,count=1, bitfieldInfo = [], enum = [])\
    ]

Messaging.Register("Peer.Temperature.Status", Peer_Temperature_Status.ID, Peer_Temperature_Status)
