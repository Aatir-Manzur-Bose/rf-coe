#    obj/CodeGenerator/Python/Peer/TTSReady.py
#    Created 27/07/2023 at 10:10:58 from:
#        Messages = messages/Peer/TTSReady.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Peer_TTSReady_Status :
    ID = 61603
    SIZE = 1
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 15), ("Function", 10), ("Operator", 3)])
    ReverseIDs = OrderedDict([(15, "FunctionBlock"), (10, "Function"), (3, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Peer_TTSReady_Status.MSG_OFFSET + Peer_TTSReady_Status.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Peer_TTSReady_Status.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Peer_TTSReady_Status.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Peer_TTSReady_Status.MSG_OFFSET + Peer_TTSReady_Status.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Peer_TTSReady_Status.ID)
            self.hdr.SetDataLength(Peer_TTSReady_Status.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Peer.TTSReady.Status"
    # Accessors
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def GetReferenceNumber(self):
        """ReferenceNumber of the TTS ready to play"""
        value = struct.unpack_from('B', self.rawBuffer(), Peer_TTSReady_Status.MSG_OFFSET + 0)[0]
        return value
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def SetReferenceNumber(self, value):
        """ReferenceNumber of the TTS ready to play"""
        tmp = min(max(value, 0), 255)
        struct.pack_into('B', self.rawBuffer(), Peer_TTSReady_Status.MSG_OFFSET + 0, tmp)
    

    # Reflection information
    fields = [ \
        FieldInfo(name="ReferenceNumber",type="int",units="",minVal="0",maxVal="255",description="ReferenceNumber of the TTS ready to play",get=GetReferenceNumber,set=SetReferenceNumber,count=1, bitfieldInfo = [], enum = [])\
    ]

Messaging.Register("Peer.TTSReady.Status", Peer_TTSReady_Status.ID, Peer_TTSReady_Status)
