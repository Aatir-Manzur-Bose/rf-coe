#    obj/CodeGenerator/Python/Notification/Reset.py
#    Created 27/07/2023 at 10:10:56 from:
#        Messages = messages/Notification/Reset.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Notification_Reset_Start :
    ID = 36885
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 9), ("Function", 1), ("Operator", 5)])
    ReverseIDs = OrderedDict([(9, "FunctionBlock"), (1, "Function"), (5, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Notification_Reset_Start.MSG_OFFSET + Notification_Reset_Start.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Notification_Reset_Start.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Notification_Reset_Start.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Notification_Reset_Start.MSG_OFFSET + Notification_Reset_Start.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Notification_Reset_Start.ID)
            self.hdr.SetDataLength(Notification_Reset_Start.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Notification.Reset.Start"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("Notification.Reset.Start", Notification_Reset_Start.ID, Notification_Reset_Start)
#    obj/CodeGenerator/Python/Notification/Reset.py
#    Created 27/07/2023 at 10:10:56 from:
#        Messages = messages/Notification/Reset.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Notification_Reset_Result :
    ID = 36886
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 9), ("Function", 1), ("Operator", 6)])
    ReverseIDs = OrderedDict([(9, "FunctionBlock"), (1, "Function"), (6, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Notification_Reset_Result.MSG_OFFSET + Notification_Reset_Result.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Notification_Reset_Result.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Notification_Reset_Result.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Notification_Reset_Result.MSG_OFFSET + Notification_Reset_Result.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Notification_Reset_Result.ID)
            self.hdr.SetDataLength(Notification_Reset_Result.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Notification.Reset.Result"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("Notification.Reset.Result", Notification_Reset_Result.ID, Notification_Reset_Result)
