#    obj/CodeGenerator/Python/Auth/VerifyChallengeResponse.py
#    Created 27/07/2023 at 10:09:45 from:
#        Messages = messages/Auth/VerifyChallengeResponse.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Auth_VerifyChallengeResponse_Start :
    ID = 73829
    SIZE = 64
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 18), ("Function", 6), ("Operator", 5)])
    ReverseIDs = OrderedDict([(18, "FunctionBlock"), (6, "Function"), (5, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Auth_VerifyChallengeResponse_Start.MSG_OFFSET + Auth_VerifyChallengeResponse_Start.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Auth_VerifyChallengeResponse_Start.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Auth_VerifyChallengeResponse_Start.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Auth_VerifyChallengeResponse_Start.MSG_OFFSET + Auth_VerifyChallengeResponse_Start.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Auth_VerifyChallengeResponse_Start.ID)
            self.hdr.SetDataLength(Auth_VerifyChallengeResponse_Start.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Auth.VerifyChallengeResponse.Start"
    # Accessors
    @msg.units('hex')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(32)
    def GetEncryptedData(self, idx):
        """Encryption of 128-bit incremented nonce, 16-bit bitfield and 14-byte PKCS #7 padding. Encryption is done using AES-128-CBC algorithm with first 128-bits of ECDHE shared secret as the Initialization Vector and the last 128 bits of ECDHE shared secret as the AES-128 encryption Key."""
        value = struct.unpack_from('B', self.rawBuffer(), Auth_VerifyChallengeResponse_Start.MSG_OFFSET + 0+idx*1)[0]
        return value
    
    @msg.units('hex')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('32')
    @msg.size('1')
    @msg.count(32)
    def GetSignature(self, idx):
        """256-bit HMAC signature of the encrypted payload.  The signature is generated using the HMAC-SHA-256 algorithm with the 256-bit ECDHE shared secret as the SHA-256 HMAC key."""
        value = struct.unpack_from('B', self.rawBuffer(), Auth_VerifyChallengeResponse_Start.MSG_OFFSET + 32+idx*1)[0]
        return value
    
    @msg.units('hex')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(32)
    def SetEncryptedData(self, value, idx):
        """Encryption of 128-bit incremented nonce, 16-bit bitfield and 14-byte PKCS #7 padding. Encryption is done using AES-128-CBC algorithm with first 128-bits of ECDHE shared secret as the Initialization Vector and the last 128 bits of ECDHE shared secret as the AES-128 encryption Key."""
        tmp = min(max(value, 0), 255)
        struct.pack_into('B', self.rawBuffer(), Auth_VerifyChallengeResponse_Start.MSG_OFFSET + 0+idx*1, tmp)
    
    @msg.units('hex')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('32')
    @msg.size('1')
    @msg.count(32)
    def SetSignature(self, value, idx):
        """256-bit HMAC signature of the encrypted payload.  The signature is generated using the HMAC-SHA-256 algorithm with the 256-bit ECDHE shared secret as the SHA-256 HMAC key."""
        tmp = min(max(value, 0), 255)
        struct.pack_into('B', self.rawBuffer(), Auth_VerifyChallengeResponse_Start.MSG_OFFSET + 32+idx*1, tmp)
    

    # Reflection information
    fields = [ \
        FieldInfo(name="EncryptedData",type="int",units="hex",minVal="0",maxVal="255",description="Encryption of 128-bit incremented nonce, 16-bit bitfield and 14-byte PKCS #7 padding. Encryption is done using AES-128-CBC algorithm with first 128-bits of ECDHE shared secret as the Initialization Vector and the last 128 bits of ECDHE shared secret as the AES-128 encryption Key.",get=GetEncryptedData,set=SetEncryptedData,count=32, bitfieldInfo = [], enum = []),\
        FieldInfo(name="Signature",type="int",units="hex",minVal="0",maxVal="255",description="256-bit HMAC signature of the encrypted payload.  The signature is generated using the HMAC-SHA-256 algorithm with the 256-bit ECDHE shared secret as the SHA-256 HMAC key.",get=GetSignature,set=SetSignature,count=32, bitfieldInfo = [], enum = [])\
    ]

Messaging.Register("Auth.VerifyChallengeResponse.Start", Auth_VerifyChallengeResponse_Start.ID, Auth_VerifyChallengeResponse_Start)
#    obj/CodeGenerator/Python/Auth/VerifyChallengeResponse.py
#    Created 27/07/2023 at 10:09:45 from:
#        Messages = messages/Auth/VerifyChallengeResponse.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class Auth_VerifyChallengeResponse_Result :
    ID = 73830
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 18), ("Function", 6), ("Operator", 6)])
    ReverseIDs = OrderedDict([(18, "FunctionBlock"), (6, "Function"), (6, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(Auth_VerifyChallengeResponse_Result.MSG_OFFSET + Auth_VerifyChallengeResponse_Result.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, Auth_VerifyChallengeResponse_Result.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, Auth_VerifyChallengeResponse_Result.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(Auth_VerifyChallengeResponse_Result.MSG_OFFSET + Auth_VerifyChallengeResponse_Result.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(Auth_VerifyChallengeResponse_Result.ID)
            self.hdr.SetDataLength(Auth_VerifyChallengeResponse_Result.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "Auth.VerifyChallengeResponse.Result"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("Auth.VerifyChallengeResponse.Result", Auth_VerifyChallengeResponse_Result.ID, Auth_VerifyChallengeResponse_Result)
