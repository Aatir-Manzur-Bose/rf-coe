#    obj/CodeGenerator/Python/EarbudDebug/Events.py
#    Created 27/07/2023 at 10:10:25 from:
#        Messages = messages/EarbudDebug/Events.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class EarbudDebug_Events_Start :
    ID = 110629
    SIZE = 1
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    EarbudEvents = OrderedDict([("CHARGER_MESSAGE_ATTACHED", 0), ("CHARGER_MESSAGE_DETACHED", 1), ("PROXIMITY_MESSAGE_START_OF_DON", 2), ("PROXIMITY_MESSAGE_IN_PROXIMITY", 3), ("PROXIMITY_MESSAGE_NOT_IN_PROXIMITY", 4), ("ACCELEROMETER_MESSAGE_IN_MOTION", 5), ("ACCELEROMETER_MESSAGE_NOT_IN_MOTION", 6)])
    ReverseEarbudEvents = OrderedDict([(0, "CHARGER_MESSAGE_ATTACHED"), (1, "CHARGER_MESSAGE_DETACHED"), (2, "PROXIMITY_MESSAGE_START_OF_DON"), (3, "PROXIMITY_MESSAGE_IN_PROXIMITY"), (4, "PROXIMITY_MESSAGE_NOT_IN_PROXIMITY"), (5, "ACCELEROMETER_MESSAGE_IN_MOTION"), (6, "ACCELEROMETER_MESSAGE_NOT_IN_MOTION")])
    IDs = OrderedDict([("FunctionBlock", 27), ("Function", 2), ("Operator", 5)])
    ReverseIDs = OrderedDict([(27, "FunctionBlock"), (2, "Function"), (5, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(EarbudDebug_Events_Start.MSG_OFFSET + EarbudDebug_Events_Start.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, EarbudDebug_Events_Start.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, EarbudDebug_Events_Start.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(EarbudDebug_Events_Start.MSG_OFFSET + EarbudDebug_Events_Start.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(EarbudDebug_Events_Start.ID)
            self.hdr.SetDataLength(EarbudDebug_Events_Start.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "EarbudDebug.Events.Start"
    # Accessors
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def GetValue(self):
        """Event"""
        value = struct.unpack_from('B', self.rawBuffer(), EarbudDebug_Events_Start.MSG_OFFSET + 0)[0]
        return value
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('0')
    @msg.count(1)
    def GetEvent(self, enumAsInt=0):
        """"""
        value = (self.GetValue() >> 0) & 0xff
        if not enumAsInt:
            value = EarbudDebug_Events_Start.ReverseEarbudEvents.get(value, value)
        return value
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def SetValue(self, value):
        """Event"""
        tmp = min(max(value, 0), 255)
        struct.pack_into('B', self.rawBuffer(), EarbudDebug_Events_Start.MSG_OFFSET + 0, tmp)
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('0')
    @msg.count(1)
    def SetEvent(self, value):
        """"""
        defaultValue = 0
        try:
            value = int(float(value))
        except ValueError:
            pass
        if isinstance(value, int) or value.isdigit():
            defaultValue = int(value)
        value = EarbudDebug_Events_Start.EarbudEvents.get(value, defaultValue)
        tmp = min(max(value, 0), 255)
        self.SetValue((self.GetValue() & ~(0xff << 0)) | ((tmp & 0xff) << 0))
    

    # Reflection information
    fields = [ \
        FieldInfo(name="Value",type="int",units="",minVal="0",maxVal="255",description="Event",get=GetValue,set=SetValue,count=1, bitfieldInfo = [\
            BitFieldInfo(name="Event",type="enumeration",units="",minVal="0",maxVal="255",description="",get=GetEvent,set=SetEvent, enum = [EarbudEvents, ReverseEarbudEvents])], enum = [])\
    ]

Messaging.Register("EarbudDebug.Events.Start", EarbudDebug_Events_Start.ID, EarbudDebug_Events_Start)
#    obj/CodeGenerator/Python/EarbudDebug/Events.py
#    Created 27/07/2023 at 10:10:25 from:
#        Messages = messages/EarbudDebug/Events.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class EarbudDebug_Events_Result :
    ID = 110630
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 27), ("Function", 2), ("Operator", 6)])
    ReverseIDs = OrderedDict([(27, "FunctionBlock"), (2, "Function"), (6, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(EarbudDebug_Events_Result.MSG_OFFSET + EarbudDebug_Events_Result.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, EarbudDebug_Events_Result.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, EarbudDebug_Events_Result.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(EarbudDebug_Events_Result.MSG_OFFSET + EarbudDebug_Events_Result.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(EarbudDebug_Events_Result.ID)
            self.hdr.SetDataLength(EarbudDebug_Events_Result.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "EarbudDebug.Events.Result"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("EarbudDebug.Events.Result", EarbudDebug_Events_Result.ID, EarbudDebug_Events_Result)
