#    obj/CodeGenerator/Python/FirmwareUpdate/SubcomponentProgress.py
#    Created 27/07/2023 at 10:10:44 from:
#        Messages = messages/FirmwareUpdate/SubcomponentProgress.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class FirmwareUpdate_SubcomponentProgress_Get :
    ID = 12449
    SIZE = 0
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    IDs = OrderedDict([("FunctionBlock", 3), ("Function", 10), ("Operator", 1)])
    ReverseIDs = OrderedDict([(3, "FunctionBlock"), (10, "Function"), (1, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(FirmwareUpdate_SubcomponentProgress_Get.MSG_OFFSET + FirmwareUpdate_SubcomponentProgress_Get.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, FirmwareUpdate_SubcomponentProgress_Get.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, FirmwareUpdate_SubcomponentProgress_Get.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(FirmwareUpdate_SubcomponentProgress_Get.MSG_OFFSET + FirmwareUpdate_SubcomponentProgress_Get.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(FirmwareUpdate_SubcomponentProgress_Get.ID)
            self.hdr.SetDataLength(FirmwareUpdate_SubcomponentProgress_Get.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "FirmwareUpdate.SubcomponentProgress.Get"
    # Accessors

    # Reflection information
    fields = [ \
    ]

Messaging.Register("FirmwareUpdate.SubcomponentProgress.Get", FirmwareUpdate_SubcomponentProgress_Get.ID, FirmwareUpdate_SubcomponentProgress_Get)
#    obj/CodeGenerator/Python/FirmwareUpdate/SubcomponentProgress.py
#    Created 27/07/2023 at 10:10:44 from:
#        Messages = messages/FirmwareUpdate/SubcomponentProgress.yaml
#        Template = Template.py
#        Language = python
#
#                     AUTOGENERATED FILE, DO NOT EDIT
import struct
import ctypes
from collections import OrderedDict
from msgtools.lib.messaging import *
import msgtools.lib.messaging as msg

class FirmwareUpdate_SubcomponentProgress_Status :
    ID = 12451
    SIZE = 2
    MSG_OFFSET = Messaging.hdrSize
    # Enumerations
    FirmwareUpdateSubcomponentStatus = OrderedDict([("Inactive", 0), ("Active", 1), ("Suspended", 2), ("Aborted", 3), ("Preparing", 4)])
    ReverseFirmwareUpdateSubcomponentStatus = OrderedDict([(0, "Inactive"), (1, "Active"), (2, "Suspended"), (3, "Aborted"), (4, "Preparing")])
    IDs = OrderedDict([("FunctionBlock", 3), ("Function", 10), ("Operator", 3)])
    ReverseIDs = OrderedDict([(3, "FunctionBlock"), (10, "Function"), (3, "Operator")])
    
    
    #@staticmethod
    #def Create():
    #    message_buffer = ctypes.create_string_buffer(FirmwareUpdate_SubcomponentProgress_Status.MSG_OFFSET + FirmwareUpdate_SubcomponentProgress_Status.SIZE)
    #
    #    Messaging.hdr.SetMessageID(message_buffer, FirmwareUpdate_SubcomponentProgress_Status.ID)
    #    Messaging.hdr.SetDataLength(message_buffer, FirmwareUpdate_SubcomponentProgress_Status.SIZE)
    #
    #    return message_buffer

    def __init__(self, messageBuffer=None):
        doInit = 0
        if messageBuffer == None:
            doInit = 1
            messageBuffer = ctypes.create_string_buffer(FirmwareUpdate_SubcomponentProgress_Status.MSG_OFFSET + FirmwareUpdate_SubcomponentProgress_Status.SIZE)
        else:
            try:
                messageBuffer.raw
            except AttributeError:
                newbuf = ctypes.create_string_buffer(len(messageBuffer))
                for i in range(0, len(messageBuffer)):
                    newbuf[i] = bytes(messageBuffer)[i]
                messageBuffer = newbuf
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        self.msg_buffer_wrapper = { "msg_buffer": messageBuffer }

        self.hdr = Messaging.hdr(messageBuffer)
        if doInit:
            self.hdr.SetMessageID(FirmwareUpdate_SubcomponentProgress_Status.ID)
            self.hdr.SetDataLength(FirmwareUpdate_SubcomponentProgress_Status.SIZE)
            self.initialize()

    def initialize(self):
            pass

    def rawBuffer(self):
        # this is a trick to get us to store a copy of a pointer to a buffer, rather than making a copy of the buffer
        return self.msg_buffer_wrapper["msg_buffer"]

    @staticmethod
    def MsgName():
        return "FirmwareUpdate.SubcomponentProgress.Status"
    # Accessors
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def GetsubcomponentUpdateStatus(self, enumAsInt=0):
        """Subcomponent update status"""
        value = struct.unpack_from('B', self.rawBuffer(), FirmwareUpdate_SubcomponentProgress_Status.MSG_OFFSET + 0)[0]
        if not enumAsInt:
            value = FirmwareUpdate_SubcomponentProgress_Status.ReverseFirmwareUpdateSubcomponentStatus.get(value, value)
        return value
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('1')
    @msg.size('1')
    @msg.count(1)
    def GetProgress(self):
        """Estimated percentage complete (0-100) relative to all subcomponets being updated.  It will not report 100 percent until all subcomponents have been completely updated (that is, it will not round up to 100 percent)."""
        value = struct.unpack_from('B', self.rawBuffer(), FirmwareUpdate_SubcomponentProgress_Status.MSG_OFFSET + 1)[0]
        return value
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('0')
    @msg.size('1')
    @msg.count(1)
    def SetsubcomponentUpdateStatus(self, value):
        """Subcomponent update status"""
        defaultValue = 0
        try:
            value = int(float(value))
        except ValueError:
            pass
        if isinstance(value, int) or value.isdigit():
            defaultValue = int(value)
        value = FirmwareUpdate_SubcomponentProgress_Status.FirmwareUpdateSubcomponentStatus.get(value, defaultValue)
        tmp = min(max(value, 0), 255)
        struct.pack_into('B', self.rawBuffer(), FirmwareUpdate_SubcomponentProgress_Status.MSG_OFFSET + 0, tmp)
    
    @msg.units('')
    @msg.default('')
    @msg.minVal('0')
    @msg.maxVal('255')
    @msg.offset('1')
    @msg.size('1')
    @msg.count(1)
    def SetProgress(self, value):
        """Estimated percentage complete (0-100) relative to all subcomponets being updated.  It will not report 100 percent until all subcomponents have been completely updated (that is, it will not round up to 100 percent)."""
        tmp = min(max(value, 0), 255)
        struct.pack_into('B', self.rawBuffer(), FirmwareUpdate_SubcomponentProgress_Status.MSG_OFFSET + 1, tmp)
    

    # Reflection information
    fields = [ \
        FieldInfo(name="subcomponentUpdateStatus",type="enumeration",units="",minVal="0",maxVal="255",description="Subcomponent update status",get=GetsubcomponentUpdateStatus,set=SetsubcomponentUpdateStatus,count=1, bitfieldInfo = [], enum = [FirmwareUpdateSubcomponentStatus, ReverseFirmwareUpdateSubcomponentStatus]),\
        FieldInfo(name="Progress",type="int",units="",minVal="0",maxVal="255",description="Estimated percentage complete (0-100) relative to all subcomponets being updated.  It will not report 100 percent until all subcomponents have been completely updated (that is, it will not round up to 100 percent).",get=GetProgress,set=SetProgress,count=1, bitfieldInfo = [], enum = [])\
    ]

Messaging.Register("FirmwareUpdate.SubcomponentProgress.Status", FirmwareUpdate_SubcomponentProgress_Status.ID, FirmwareUpdate_SubcomponentProgress_Status)
